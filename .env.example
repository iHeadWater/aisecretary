# 数据库文件路径，默认存放在用户主目录下（repo 外）
# 备份由 myopenclaw 的 backup-cron 统一管理
# 如需自定义路径，取消注释并修改
# DATABASE_PATH=/custom/path/to/transactions.sqlite
# 使用 ~ 代表用户主目录，config.py 会自动展开
DATABASE_PATH=~/.myagentdata/aisecretary/transactions.sqlite

